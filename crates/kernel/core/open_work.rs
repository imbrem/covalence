//! Source-local open work for this module/crate.
//!
//! Migrated from the former Markdown registry. Move markers beside the relevant
//! implementation as the area is touched; `bun run todos` provides the index.

// TODO(cov:kernel.core.src-facts-rs, major): src/facts.rs: empty module. The observer layer recording and
// TODO(cov:kernel.core.legacy-prover, major): Legacy prover: the arena + e-graph + union-find prover kernel was removed
// TODO(cov:kernel.core.async-dependency-loading, major): Async dependency loading: ArticleSource + TrustPolicy are defined as
// TODO(cov:kernel.core.trustlevel-deepcheck-not-enforced, major): TrustLevel::DeepCheck not enforced: both levels behave identically (stdlib
// TODO(cov:kernel.core.no-diagnostic-spans, major): No diagnostic spans: Diagnostic::span always None until the .cov script
// TODO(cov:kernel.core.statement-rendering-is-canonical-s-expression, major): Statement rendering is canonical S-expression: , not math notation; MathML
