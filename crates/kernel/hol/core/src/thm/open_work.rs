//! Source-local open work for this module/crate.
//!
//! Migrated from the former Markdown registry. Move markers beside the relevant
//! implementation as the area is touched; `bun run todos` provides the index.

// TODO(cov:kernel.hol.core.src.thm.permanent-structural-tohol-unfolding-rules-not-yet-admitted, minor): Permanent structural toHOL unfolding rules not yet admitted
// TODO(cov:kernel.hol.core.src.thm.the-val-rules-the-family-certs-lit-toterm-are-transitional, minor): The Val rules + the family certs' Lit::toterm are TRANSITIONAL
// TODO(cov:kernel.hol.core.src.thm.symbolic-conclusion-mechanism-thm-l-c-remaining-float-ops, minor): Symbolic-conclusion mechanism (Thm<L, C>): remaining float ops
// TODO(cov:kernel.hol.core.src.thm.frompuresym-trusts-the-mint-no-non-forcing-floor, minor): frompuresym trusts the mint (no non-forcing floor): It cannot re-run
// TODO(cov:kernel.hol.core.src.thm.no-native-pure-hol-embedding, minor): No native pure-HOL embedding: IsThm carries (Ctx, Term) as opaque Val
// TODO(cov:kernel.hol.core.src.thm.unfoldtermspec-survives-to-the-defs-re-home, minor): UnfoldTermSpec survives to the defs re-home: Sound definitional
// TODO(cov:kernel.hol.core.src.thm.with-interning-is-post-hoc, minor): with interning is post-hoc: The cons-aware with methods mint via the
// TODO(cov:kernel.hol.core.src.thm.no-sigma-a-p, minor): No Sigma<A, P>: A proof-carrying value (an Expr<A> bundled with a theorem
