//! Source-local open work for this module/crate.
//!
//! Migrated from the former Markdown registry. Move markers beside the relevant
//! implementation as the area is touched; `bun run todos` provides the index.

// TODO(cov:lib.wasm.spectec.native-watsup-frontend, major): Native .watsup frontend: The hand-written lexer/parser/CST/elaborator/
// TODO(cov:lib.wasm.spectec.wasm-grammar-versions, major): WASM grammar versions: (src/grammar.rs). Only grammar::wasm3 is wired
// TODO(cov:lib.wasm.spectec.cfg-lowering-recognition-mode-residue, major): CFG lowering — recognition-mode residue: (src/cfg.rs). Two modes
// TODO(cov:lib.wasm.spectec.byte-regex-bridge-scope, major): Byte-regex bridge scope: (regex.rs). regex::symtoregexu8 bridges only
