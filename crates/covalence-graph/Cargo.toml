[package]
name = "covalence-graph"
version = "0.0.0"
edition = "2024"

[dependencies]
bytes = "1"
thiserror = "2"
covalence-hash = { path = "../lib/hash" }
covalence-parse = { path = "../lib/parse" }
