// K tutorial Lesson 1.2 (02_basics) — colorOf / contentsOfJar, transcribed.
// Function rules fired anywhere via congruence; the exact first-order shape.
module COLORS
  rule colorOf(Banana()) => Yellow()
  rule colorOf(Blueberry()) => Blue()
  rule contentsOfJar(Jar(F)) => F
endmodule
