//! Source-local open work for this module/crate.
//!
//! Migrated from the former Markdown registry. Move markers beside the relevant
//! implementation as the area is touched; `bun run todos` provides the index.

// TODO(cov:kernel.hol.init.src.grammar.regex.provemember-one-time-55s-warm-up, severe): provemember one-time ~55s warm-up: First call per process builds the
// TODO(cov:kernel.hol.init.src.grammar.regex.recognizer-acceleration, minor): Recognizer acceleration: The pure-Rust tactic::Recognizer checks only simple
// TODO(cov:kernel.hol.init.src.grammar.regex.no-subtree-interner, minor): No subtree interner: Core holds sub-regexes behind Arc for sharing, but
// TODO(cov:kernel.hol.init.src.grammar.regex.proveword-variable-matching-is-structural-not-unifying, minor): proveword variable matching is structural, not unifying: A variable token is
// TODO(cov:kernel.hol.init.src.grammar.regex.word-normalisation-deferred, minor): Word normalisation deferred: The word w in Matches ⌜r⌝ w is rule-shaped
