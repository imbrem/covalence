//! Source-local open work for this module/crate.
//!
//! Migrated from the former Markdown registry. Move markers beside the relevant
//! implementation as the area is touched; `bun run todos` provides the index.

// TODO(cov:kernel.hol.init.src.spectec.only-rel-gram-fragment-kinds, minor): Only Rel + Gram fragment kinds: RelationEnv (relations) and
// TODO(cov:kernel.hol.init.src.spectec.relation-coverage-engine-coverage, minor): Relation coverage = engine coverage: RelationEnv::spec lowers only rules
// TODO(cov:kernel.hol.init.src.spectec.no-state-in-the-fragment-trait, minor): No state in the Fragment trait: Judgement statement differs per kind
