//! Source-local open work for this module/crate.
//!
//! Migrated from the former Markdown registry. Move markers beside the relevant
//! implementation as the area is touched; `bun run todos` provides the index.

// TODO(cov:kernel.sat.rat-steps-unhandled, severe): RAT steps unhandled: (parser + replay). Non---plain CaDiCaL proofs, and
// TODO(cov:kernel.sat.interning-hash-consing, minor): Interning/hash-consing: on the sequent backend is a further constant-factor
// TODO(cov:kernel.sat.sequentclausebackend-isemptyclause-is-best-effort, minor): SequentClauseBackend::isemptyclause is best-effort: A unit input
// TODO(cov:kernel.sat.drat-no-hints-not-handled, minor): DRAT (no hints) not handled: Only LRAT (with per-step antecedents) is a
