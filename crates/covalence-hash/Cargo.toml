[package]
name = "covalence-hash"
version = "0.0.0"
edition = "2024"

[features]
default = ["git"]
git = ["dep:gix-hash"]

[dependencies]
blake3 = "1"
gix-hash = { version = "0.25", features = ["sha1", "sha256"], optional = true }
rand = "0.9"
sha2 = "0.10"
