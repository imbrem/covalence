{
	"private": true,
	"name": "server",
	"displayName": "The Covalence Language Server binary",
	"version": "0.1.0",
	"author": "Jad Elkhaleq Ghalayini",
	"license": "MIT",
	"scripts": {
		"build": "cargo rustc --release --target wasm32-wasip1-threads -- -Clink-arg=--initial-memory=10485760 -Clink-arg=--max-memory=10485760 && mkdir -p dist && cp ../target/wasm32-wasip1-threads/release/covalence-lsp.wasm ./dist/covalence-lsp.wasm",
		"package": "bun run build",
		"test:wasm": "node ./bin/send.js | wasmtime --wasm threads --wasi threads ./dist/covalence-lsp.wasm"
	}
}