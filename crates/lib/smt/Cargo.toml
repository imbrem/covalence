[package]
name = "covalence-smt"
version = "0.0.0"
edition = "2024"

[features]
cvc5 = []

[dependencies]
covalence-types = { path = "../types" }
covalence-sat = { path = "../sat" }
covalence-sexp = { path = "../sexp" }
thiserror = "2"

[package.metadata.cargo-machete]
# Intentional layering (SMT = SAT + theories); no code use yet, wired when solving lands.
ignored = ["covalence-sat"]
