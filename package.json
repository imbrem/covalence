{
  "name": "covalence",
  "private": true,
  "workspaces": ["extensions/*", "apps/*", "packages/*"],
  "devDependencies": {
    "covalence-client": "workspace:*"
  },
  "scripts": {
    "build": "bun run build:web && bun run build:native && bun run build:wasm",
    "build:native": "cargo build -p covalence",
    "build:wasm": "bun run --filter covalence-vscode build",
    "build:web": "bun run build:web-kernel && bun run --filter covalence-web build",
    "build:web-kernel": "cargo build -p covalence-web-kernel --target wasm32-unknown-unknown --release && wasm-bindgen target/wasm32-unknown-unknown/release/covalence_web_kernel.wasm --out-dir apps/covalence-web/src/lib/kernel --target web --omit-default-module-path && wasm-opt -Oz --enable-reference-types --enable-bulk-memory --enable-nontrapping-float-to-int -o apps/covalence-web/src/lib/kernel/covalence_web_kernel_bg.wasm apps/covalence-web/src/lib/kernel/covalence_web_kernel_bg.wasm",
    "build:serve": "bun run build:web && cargo build -p covalence",
    "dev:web": "bun run --filter covalence-web dev",
    "serve": "bun run build:native && bun run build:web-kernel && bun scripts/dev-serve.mjs",
    "dev:serve": "echo 'Run cov serve --api in another terminal, then:' && bun run dev:web",
    "release": "bun run build:web && bun run release:native && bun run release:wasm",
    "release:native": "cargo build -p covalence --release",
    "release:wasm": "bun run build:wasm",
    "dev": "echo 'Use: bun run code:browser or bun run code:desktop' && exit 1",
    "code:browser": "bun run build:wasm && bun run --filter covalence-vscode code:browser",
    "code:desktop": "bun run build && bun run --filter covalence-vscode code:desktop",
    "check": "cargo check",
    "build:python": "cd crates/covalence-python && source activate.sh && maturin develop",
    "test:python": "cd crates/covalence-python && source activate.sh && pytest tests/ -v",
    "test:ui": "bun run --filter covalence-ui test",
    "test:wasm-js": "bun run --filter covalence-wasm-js test",
    "build:wasm-build-guest": "cargo build --target wasm32-unknown-unknown --release -p covalence-wasm-build-guest && cp target/wasm32-unknown-unknown/release/covalence_wasm_build_guest.wasm packages/covalence-wasm-js/src/__fixtures__/build_guest.wasm"
  }
}
