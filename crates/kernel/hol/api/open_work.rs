//! Source-local open work for this module/crate.
//!
//! Migrated from the former Markdown registry. Move markers beside the relevant
//! implementation as the area is touched; `bun run todos` provides the index.

// TODO(cov:kernel.hol.api.holomega-is-type-only-on-the-reflected-rep, major): HolOmega is TYPE-only + on the REFLECTED rep: src/omega.rs exposes the
// TODO(cov:kernel.hol.api.trait-family-incomplete, major): Trait family incomplete: Hol + Nat + Int + HolOmega exist. Planned
// TODO(cov:kernel.hol.api.succhol-succdischarger-are-nat-only, major): SuccHol/SuccDischarger are nat-only: src/succ.rs lands the
// TODO(cov:kernel.hol.api.hol-error-type-not-associated, major): Hol error type not associated: Fixed to covalencecore::Result
