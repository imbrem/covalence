//! Source-local open work for this module/crate.
//!
//! Migrated from the former Markdown registry. Move markers beside the relevant
//! implementation as the area is touched; `bun run todos` provides the index.

// TODO(cov:kernel.hol.init.src.rust-cov-mutual-recursion, major): Rust↔.cov mutual recursion: Cycles through the Rust↔.cov boundary are
// TODO(cov:kernel.hol.init.src.concurrent-unit-compilation, major): Concurrent unit compilation: Units compile strictly sequentially
// TODO(cov:kernel.hol.init.src.single-init-mod-rs-project, major): Single init/mod.rs Project: libraryenv / librarytactic /
// TODO(cov:kernel.hol.init.src.wasm-project-units-cargo-features-distribution, major): WASM project units + Cargo-features distribution: Compiling a Rust unit to
