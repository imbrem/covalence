[package]
name = "covalence-lsp"
version = "0.0.0"
edition = "2024"

[lib]
crate-type = ["rlib"]

[dependencies]
covalence-sexp = { path = "../lib/sexp" }
covalence-wasm = { path = "../lib/wasm/core" }
lsp-types = "0.97"
lsp-server = "0.7"
serde_json = "1"
