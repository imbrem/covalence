//! Source-local open work for this module/crate.
//!
//! Migrated from the former Markdown registry. Move markers beside the relevant
//! implementation as the area is touched; `bun run todos` provides the index.

// TODO(cov:kernel.hol.init.src.grammar.spectec.exact-under-mode-bmodule, minor): Exact (Under-mode) Bmodule: Whole-module parsing is RECOGNITION-only
// TODO(cov:kernel.hol.init.src.grammar.spectec.t-identifier-context-params, minor): T identifier-context params: Recognition mode monomorphises integer
// TODO(cov:kernel.hol.init.src.grammar.spectec.rule-let-premises, minor): rule/let premises: classifypremises drops ifs and
// TODO(cov:kernel.hol.init.src.grammar.spectec.2-bridge-terminals, minor): 2 bridge terminals: Two T-side terminal fragments fall outside the
// TODO(cov:kernel.hol.init.src.grammar.spectec.faithful-listn, minor): Faithful ListN: Under-approx skips parametric-length vectors;
// TODO(cov:kernel.hol.init.src.grammar.spectec.whole-module-tactic-cost, minor): Whole-module tactic cost: T5/T5b run on the 64 MiB
// TODO(cov:kernel.hol.init.src.grammar.spectec.wasm-1-0-2-0, minor): WASM 1.0 / 2.0: Only the 3.0 dump is bundled; validated 1.0/2.0 SpecTec AST
