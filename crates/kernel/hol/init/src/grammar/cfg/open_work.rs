//! Source-local open work for this module/crate.
//!
//! Migrated from the former Markdown registry. Move markers beside the relevant
//! implementation as the area is touched; `bun run todos` provides the index.

// TODO(cov:kernel.hol.init.src.grammar.cfg.s2-comprehension-family-least-ness, minor): S2 comprehension family least-ness: The full fixpoint characterisation
// TODO(cov:kernel.hol.init.src.grammar.cfg.s3-at-scale-per-env-discharge-cost, minor): S3 at scale / per-env discharge cost: derivesinfamilyregular discharges
// TODO(cov:kernel.hol.init.src.grammar.cfg.env-transport-for-version-inclusion-metatheorems, minor): Env transport for version-inclusion metatheorems: ⊢ DerivesE n w ⟹
// TODO(cov:kernel.hol.init.src.grammar.cfg.tactic-recognizer-acceleration-tactic-rs, minor): Tactic recognizer acceleration (tactic.rs): The phase-1 recognizer is a
// TODO(cov:kernel.hol.init.src.grammar.cfg.terminal-sub-span-plan-reuse-tactic-rs, minor): Terminal sub-span plan reuse (tactic.rs): Terminal segments are
// TODO(cov:kernel.hol.init.src.grammar.cfg.word-flattening, minor): Word flattening: Conclusion words are rule-shaped cat/cons/nil
// TODO(cov:kernel.hol.init.src.grammar.cfg.first-class-grammar-values, minor): First-class grammar values: Derives is per-env (grammar baked into the
