//! Source-local open work for this module/crate.
//!
//! Migrated from the former Markdown registry. Move markers beside the relevant
//! implementation as the area is touched; `bun run todos` provides the index.

// TODO(cov:kernel.hol.init.src.models.no-general-signature-admits, severe): No general Signature/admits: Logic::natmodel() is specialized to the
// TODO(cov:kernel.hol.init.src.models.no-model-synthesis-3-0-4-north-star, severe): No model synthesis (§3.0.4 north star): Models are hand-declared via
// TODO(cov:kernel.hol.init.src.models.partial-handlerset, minor): Partial HandlerSet: Only the induction handler (m.induct) dispatches per
// TODO(cov:kernel.hol.init.src.models.no-typed-sig-thy-mod-imports, minor): No typed .sig/.thy/.mod imports: (#import x.sig #sig) (§3.0/§3.0.1)
// TODO(cov:kernel.hol.init.src.models.from-witness-is-transitional, minor): (from WITNESS) is transitional: (#models nat/unary …) pulls specs from the
// TODO(cov:kernel.hol.init.src.models.models-registers-no-thm-certificate, minor): #models registers no .thm certificate: Verified axioms accumulate as
// TODO(cov:kernel.hol.init.src.models.in-model-only-accepts-thm, minor): #in MODEL only accepts #thm: Nested #def/#have/#in, #transport
// TODO(cov:kernel.hol.init.src.models.two-models-one-theory-no-iso, minor): Two models, one theory, no iso: Nat has nat/self + nat/unary with no
