//! Source-local open work for this module/crate.
//!
//! Migrated from the former Markdown registry. Move markers beside the relevant
//! implementation as the area is touched; `bun run todos` provides the index.

// TODO(cov:kernel.hol.init.src.metalogic.structural-for-transport-mm-import-nat-carrier-still-open-tier-2, severe): Structural σ for transport — MM-import Φ=nat carrier still open (TIER 2)
// TODO(cov:kernel.hol.init.src.metalogic.hol-zfc-scale-transport, severe): HOL→ZFC-scale transport: (DerivableHOL ⟹ DerivableZFC ∘ σ) — the
// TODO(cov:kernel.hol.init.src.metalogic.p-validproof-p-s-db-derivabledb-db-s-grounding-bridge, severe): ∃P. ValidProof(P,S,db) ⟺ DerivableDB db S grounding bridge: DerivableDB
// TODO(cov:kernel.hol.init.src.metalogic.lift-scoped-l-l-to-full-l, minor): Lift scoped L' ⊆ L to full L: derivetheorem yields DerivableL';
// TODO(cov:kernel.hol.init.src.metalogic.typecodes-d-over-approximated, minor): Typecodes & $d over-approximated: (sound for construct direction): clauses
// TODO(cov:kernel.hol.init.src.metalogic.per-logic-denotation, minor): Per-logic denotation: for projecting a finished DerivableL to a concrete
// TODO(cov:kernel.hol.init.src.metalogic.declarations-only-load-prove-on-demand, minor): Declarations-only load + prove-on-demand: Parse keeping only declarations
// TODO(cov:kernel.hol.init.src.metalogic.structured-former-encoding, minor): Structured-former encoding: (shorter HOL repr): give each syntactic former a
// TODO(cov:kernel.hol.init.src.metalogic.set-mm-zfc-stdlib-program, minor): set.mm/ZFC stdlib program: (design): use set.mm as ZFC stdlib from the
// TODO(cov:kernel.hol.init.src.metalogic.s-transport, major): S-transport: DerivableL1(A) ⟹ DerivableL2(S(A)) (§5.6) — ⟹σ
// TODO(cov:kernel.hol.init.src.metalogic.conservative-extension-equivalence-a-b, major): Conservative extension / equivalence A ≅ B: mutual interpretation; the
// TODO(cov:kernel.hol.init.src.metalogic.metamath-l-native-l, major): Metamath-L ≅ native-L: (§5.6). The composition side is now built
// TODO(cov:kernel.hol.init.src.metalogic.full-logic-directive-wiring, major): Full #logic directive wiring: into the .cov/surface compiler (§5.6)
