//! Source-local open work for this module/crate.
//!
//! Migrated from the former Markdown registry. Move markers beside the relevant
//! implementation as the area is touched; `bun run todos` provides the index.

// TODO(cov:kernel.hol.init.src.script.async-holes-channels-removed-pending-channel-based-rebuild, severe): Async holes/channels removed, pending channel-based rebuild: The
// TODO(cov:kernel.hol.init.src.script.dep-name-is-a-sync-availability-guard-not-an-await, severe): (#dep NAME) is a sync availability guard, not an await: Errors if NAME
// TODO(cov:kernel.hol.init.src.script.scripterror-is-flat-strings-no-source-spans-or-traces, severe): ScriptError is flat strings — no source spans or traces: Should carry source
// TODO(cov:kernel.hol.init.src.script.typed-pipeline-extents-missing, severe): Typed pipeline + extents missing: runasync parses directives into a typed
// TODO(cov:kernel.hol.init.src.script.term-futures-term-level-holes-not-represented, severe): Term futures (term-level holes) not represented: Terms are built eagerly
// TODO(cov:kernel.hol.init.src.script.lookupconst-lookuptactic-lookuprule-still-sync, severe): lookupconst/lookuptactic/lookuprule still sync: (getready peeks)
// TODO(cov:kernel.hol.init.src.script.slow-cov-files, major): Slow .cov files: covtheory! is timed by COVPROFILE=1. Pathologically slow
// TODO(cov:kernel.hol.init.src.script.inference-is-best-effort-untrusted, minor): Inference is best-effort (untrusted): infer.rs HM unification is incomplete and
// TODO(cov:kernel.hol.init.src.script.pluggable-unifier-not-built, minor): Pluggable unifier not built: apply/rw do first-order matching via
// TODO(cov:kernel.hol.init.src.script.no-proof-term-pretty-printer-serialization-out, minor): No proof/Term pretty-printer (serialization-out): script only parses + replays;
// TODO(cov:kernel.hol.init.src.script.rw-does-not-descend-under-binders, minor): rw does not descend under binders: rewriteconv (script/drv.rs) rewrites
// TODO(cov:kernel.hol.init.src.script.prelude-env-core-covers-only-logic-nat, minor): Prelude Env::core() covers only logic + nat: int/rat/real/list/option/prod/coprod/set
// TODO(cov:kernel.hol.init.src.script.no-wasm-path-for-opt-in-parallelism, minor): No wasm path for opt-in parallelism: blockon is cfg-split (native tokio
// TODO(cov:kernel.hol.init.src.script.no-wasm-wit-kernel-api, minor): No WASM/WIT kernel API: Authoring proofs in WASM guests and importing them through a
// TODO(cov:kernel.hol.init.src.script.comp-calc-handles-only, minor): #comp calc handles only =: The heterogeneous form (a = b ≤ c < d ⊢ a < d,
// TODO(cov:kernel.hol.init.src.script.equational-seams-not-yet-registerable-per-logic, minor): Equational seams not yet registerable per-logic: Env::beta/congr/funext/
// TODO(cov:kernel.hol.init.src.script.inductive-realises-only-the-nat-shape, minor): #inductive realises only the nat shape: script/inductive.rs dispatches through
// TODO(cov:kernel.hol.init.src.script.def-catalogue-resolution-path-not-audited, minor): #def catalogue-resolution path not audited: A #def NAME whose body references
// TODO(cov:kernel.hol.init.src.script.sig-thy-model-models-scope, minor): #sig/#thy/#model/#models scope: (script/theory.rs): single-sorted/first-order
