# Skeletons — covalence-python

See [`CLAUDE.md`](../../../CLAUDE.md) § Skeletons and the [root index](../../../SKELETONS.md).

- **HOL kernel bindings removed pending rewrite** — the `pure` module exposing
  `Type`/`Term`/`Thm` was removed in the kernel rewrite (see `src/lib.rs`
  comment); reinstate on the new kernel stack.
