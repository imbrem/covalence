//! Source-local open work for this module/crate.
//!
//! Migrated from the former Markdown registry. Move markers beside the relevant
//! implementation as the area is touched; `bun run todos` provides the index.

// TODO(cov:lib.data.multiformat.codecs-are-unregistered-private-use-codes, minor): Codecs are unregistered private-use codes: codec::{AXIOMSET, COVHOLTHM,
// TODO(cov:lib.data.multiformat.no-signed-envelopes, minor): No signed envelopes: Federation / authored Thm exchange needs ed25519
// TODO(cov:lib.data.multiformat.multihash-is-blake3-only, minor): Multihash is blake3-only: Cid::parse rejects every other multihash code
// TODO(cov:lib.data.multiformat.ingest-does-not-re-derive-the-proof, minor): Ingest does not re-derive the proof: examples/kernelingest parses the
// TODO(cov:lib.data.multiformat.hol-payload-limited-to-the-round-trippable-term-fragment, minor): HOL payload limited to the round-trippable term fragment: covalence-init's
// TODO(cov:lib.data.multiformat.acset-validation-is-structural-by-design, minor): ACSet validation is structural by design: acset::validatestore checks
// TODO(cov:lib.data.multiformat.payloads-are-opaque-bytes, minor): Payloads are opaque bytes: prop / witness payloads are not typed or
