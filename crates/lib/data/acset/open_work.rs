//! Source-local open work for this module/crate.
//!
//! Migrated from the former Markdown registry. Move markers beside the relevant
//! implementation as the area is touched; `bun run todos` provides the index.

// TODO(cov:lib.data.acset.only-pullback-migration, minor): Only Δ (pullback) migration: Instance::pullback implements the pullback
// TODO(cov:lib.data.acset.compile-time-names-only, minor): Compile-time names only: Objects/morphisms/attributes are &'static str,
// TODO(cov:lib.data.acset.query-datalog-evaluation-is-naive-positive-only, minor): Query/Datalog evaluation is naive; positive only: query (conjunctive)
// TODO(cov:lib.data.acset.lattice-lfp-assumes-monotonicity-termination, minor): lattice::lfp assumes monotonicity + termination: The step's monotonicity
// TODO(cov:lib.data.acset.no-co-limits-or-schema-composition, minor): No (co)limits or schema composition: Acyclicity is an instance-level check
