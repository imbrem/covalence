//! Source-local open work for this module/crate.
//!
//! Migrated from the former Markdown registry. Move markers beside the relevant
//! implementation as the area is touched; `bun run todos` provides the index.

// TODO(cov:kernel.hol.init.src.init.acl2.s7-recursion-tiers-not-started, severe): Complete S7 body compilation and W4–W6 selection/equation proofs before enabling measured or mutual defun admission.
// TODO(cov:kernel.hol.init.src.init.acl2.deduction-compiler-blowup-is-quadratic-ish, minor): Deduction-compiler blowup is quadratic-ish: per discharged
// TODO(cov:kernel.hol.init.src.init.acl2.s4-formal-name-restriction, minor): S4 formal-name restriction: formals may not be b/h/t/sg or
// TODO(cov:kernel.hol.init.src.init.acl2.defun-foldground-covers-the-s4-gate-fragment-only, minor): defun::foldground covers the S4 gate fragment only: values,
