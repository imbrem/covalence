[package]
name = "covalence-pure-trusted"
version = "0.0.0"
edition = "2024"

[dependencies]
covalence-types = { path = "../../covalence-types" }
serde = { version = "1", features = ["derive"], optional = true }

[features]
serde = ["dep:serde"]
