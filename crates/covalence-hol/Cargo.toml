[package]
name = "covalence-hol"
version = "0.0.0"
edition = "2024"

[features]
default = []

[dependencies]
covalence-hash = { path = "../covalence-hash", default-features = false }
covalence-pure = { path = "../covalence-pure" }
smol_str = "0.3"
thiserror = "2"

[dev-dependencies]
bytes = "1"
