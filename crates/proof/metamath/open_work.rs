//! Source-local open work for this module/crate.
//!
//! Migrated from the former Markdown registry. Move markers beside the relevant
//! implementation as the area is touched; `bun run todos` provides the index.

// TODO(cov:proof.metamath.definitional-extension-conservativity, major): Definitional-extension conservativity: checkimplication / interpretation
// TODO(cov:proof.metamath.derived-witness-bridges, major): Derived-witness bridges: Implication/interpretation witnesses are existing
// TODO(cov:proof.metamath.d-reshaping-statement-matching, major): $d-reshaping statement matching: samestatementmodrenaming
// TODO(cov:proof.metamath.kernel-level-transport, major): Kernel-level transport: Implication/InterpretationCert are Rust-checked
// TODO(cov:proof.metamath.hol-backed-databasesink-over-an-arbitrary-database, major): HOL-backed DatabaseSink over an arbitrary database: A HOL
// TODO(cov:proof.metamath.set-mm-scale-streaming-incremental-parsing, major): set.mm-scale streaming/incremental parsing: The reader tokenises the whole
// TODO(cov:proof.metamath.structured-tree-grammar-parsed-expression-encoding, major): Structured-tree (grammar-parsed) expression encoding: Expr is a typecode +
// TODO(cov:proof.metamath.symbol-interning-for-set-mm-scale, major): Symbol interning for set.mm scale: Symbol is an owned SmolStr; tables
// TODO(cov:proof.metamath.emitter-fidelity-emit-rs, major): Emitter fidelity (emit.rs): Database::tommstring re-parses to a
