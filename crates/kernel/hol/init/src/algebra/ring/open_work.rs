//! Source-local open work for this module/crate.
//!
//! Migrated from the former Markdown registry. Move markers beside the relevant
//! implementation as the area is touched; `bun run todos` provides the index.

// TODO(cov:kernel.hol.init.src.algebra.ring.coefficient-collection, major): coefficient collection: like monomials are not combined: x + x stays
// TODO(cov:kernel.hol.init.src.algebra.ring.negation-subtraction, major): negation / subtraction: neg / sub are treated as opaque atoms, not
// TODO(cov:kernel.hol.init.src.algebra.ring.literal-folding-inside-monomials, major): literal folding inside monomials: (e.g. 2·3·x → 6·x)
